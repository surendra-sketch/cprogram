#include<stdio.h>
int main(){
    // Declaration of arrays
//    int a[];//error

// int a[5];//it compalsury to metion the size if not initialising //insise the braktets 5 is the size of the array not the index value.

// a[0]=9; // but at rest index will contains garbage value

//  [0,1,2,3,4] index always size-1 of the array

// we can initialise later but in that case array containt garbage value

// int a[5]={5,8,2,5,9}//valid
// int a[ ]={5,8,2,5,9}//valid

// int a[5]={3,4,2,5,3,6,7}// invalid because size of array less than the asinged elements

// int a[5]={3,4}//valid but rest index contains 0 value not garbage

    return 0;
}